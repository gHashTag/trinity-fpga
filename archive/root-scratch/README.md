# Root scratch Zig files

One-off `test_*.zig` snippets that were sitting in the repository root have been moved here so the root stays tidy. They are **not** wired into `build.zig` unless you add them.
