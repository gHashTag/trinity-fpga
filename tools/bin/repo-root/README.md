# Legacy binaries (moved from repo root)

These executables were **tracked at the repository root** and have been moved here to reduce clutter.

Prefer **`zig build <target>`** and run from **`zig-out/bin/`** for current artifacts.

Do not add new binaries here unless you intentionally snapshot a build for CI/docs.
