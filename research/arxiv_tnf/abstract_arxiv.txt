A ternary node has three sites that could carry a number format; one needs to. The weight is a code: multiplying by it is a sign-select. The sample arrives ADC-native. The accumulator alone has a range to spend, and Ternary Network Floats spend it -- a sign, a balanced-ternary exponent of Et trits, an M-bit mantissa, 1+Et+M=N. The weight is GFTernary, a two-bit \-phi,0,+phi\ alphabet, and it is where the multiplier is decided. Removing one rather than cheapening it requires the weight lattice to be closed under weight application and accumulation alike. Z[phi] is, because phi^2=phi+1: applying a weight is the Fibonacci step (a,b)->(b,a+b), accumulation is componentwise, and a layer's linear path is exact. Machine-checked in Coq. Closure does not single out phi; enumeration does. A scale is multiplier-free exactly when it is an algebraic integer whose companion matrix has entries in \0,1\. Degree two admits phi alone, and nothing lies between the shift and phi. The family r^d=r+1 reaches any granularity at one addition and d registers: fineness costs registers, not adders. Among the twenty formats carrying their own range -- those a training run can live in -- GFTernary leads on throughput per area, by 10.2% over the next and 6.1x over posit32, with eight of the twenty ours. Formats needing an external learned scale are tabulated apart. On an XC7A200T a ternary neuron costs 28 LUT per weight and no DSP at any fan-in. Which rung follows from the budget -- phi at four bits, r^5=r^3+1 at five, r^6=r+1 at six -- reaching 3.7% of fp32 at six bits and one adder. On the block axis the element is closed, the best eight-level codebook beating a deployed one by 0.9%, and the scale is not: a phi grid at four bits beats MXFP4 on two models at 0.125 fewer bits per weight. Five retractions are marked in place below.
