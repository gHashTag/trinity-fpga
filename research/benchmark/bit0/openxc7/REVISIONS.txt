bench-openxc7 provisioning record (2026-08-15)
==============================================
Same-machine benchmark row: openXC7 flow inside the frozen regymm/openxc7
container, nextpnr-xilinx rebuilt IN-IMAGE at the frozen revision.
NOTE: this file is a provisioning record. The harness (bench-openxc7.sh) does
NOT trust it -- it re-reads every revision from the built tree at run time and
stamps those into the results.

Container image
---------------
docker.io/regymm/openxc7@sha256:eced1cdd4727549f2d983328e0cf170fb6f6f67d87f19b2bf24365163368c70c
- Obtained by direct `podman pull` by digest on bit0 (Docker Hub egress works;
  the docker daemon socket is root:docker and denied to user builder, so
  rootless podman 4.9.3 is used throughout).
- Digest verified after pull: `podman image inspect .RepoDigests` returns
  exactly the digest above. Size 8768671761 bytes.
- Supplies: Ubuntu 20.04.6, yosys 0.62 (git sha1 7326bb7d6), prjxray tools
  (fasm2frames from /prjxray/env, xc7frames2bit), pypy3 7.3.1, cmake 3.16.3,
  g++ 9.4.0, boost 1.71 (libboost_python38), eigen3.
- The image's own nextpnr-xilinx (/usr/local/bin, "Version 45a986b") is NOT
  used: PATH puts the frozen in-tree build first, and the harness aborts if
  `command -v nextpnr-xilinx` resolves outside /bench/src.

nextpnr-xilinx source (src/nextpnr-xilinx)
------------------------------------------
- openXC7/nextpnr-xilinx revision 05aaa06bce455ae6ed01efa1616ba7d3a9421ca7
  (stable-backports tip 2026-08-14, "Merge pull request #146 from
  cavearr/fix/slice-egress-validity").
- bit0 has no GitHub egress: the tree was cloned on the dev mac
  (git clone --reference <local clone> --dissociate, self-contained .git kept
  so revisions stay tree-readable) and rsync'd to bit0.
- Submodules populated at the frozen checkout:
    xilinx/external/prjxray-db          d17fea26ebe46119606e875f012b9be122538aec
      (matches the gitlink recorded by 05aaa06b -- verified with
       `git ls-tree HEAD` == submodule `rev-parse HEAD`)
    xilinx/external/nextpnr-xilinx-meta a4af910cac907f2cbd3a545f26f8e26573e860de
  The `tests` submodule is not populated (not needed to build).
- Built INSIDE the image (never on the host) with the frozen recipe:
    cmake . -DARCH=xilinx -DCMAKE_BUILD_TYPE=Release && make -j$(nproc)
  via scripts/build-nextpnr.sh, podman run --network=none.

chipdb (chipdb/)
----------------
Generated in-image, outside any timed window, by scripts/gen-chipdb.sh:
image pypy3 runs xilinx/python/bbaexport.py from the frozen tree; assembled
with the bbasm BUILT FROM THE SAME TREE (blob format must match the consumer).
Parts (from the frozen designs' Makefiles; .bin named per openXC7.mk DBPART):
    xc7a35tcsg324-1   -> chipdb/xc7a35tcsg324.bin   (blinky, artix7)
    xc7k160tffg676-2  -> chipdb/xc7k160tffg676.bin  (picosoc BOARD=kx2, kintex7)
    xc7s50csga324-1   -> chipdb/xc7s50csga324.bin   (litex-ddr-arty-s7 main
                                                     and deephier, spartan7)

Benchmark designs (designs/)
----------------------------
Copied (cp -a) from the frozen staging at ~/data/openxc7/bench-vivado/designs/
(bench-vivado itself untouched, reused read-only):
    designs/main/     <- openXC7/demo-projects main@547ffc59a398529041f8829f05b8887e41955150
                         blinky-digilent-arty, picosoc, litex-ddr-arty-s7,
                         vexriscv (supplies pre-generated VexRiscv.v to litex)
    designs/deephier/ <- openXC7/demo-projects litex-ddr-arty-s7-deephier@f08d9e7bf364c9f5746825c1bdb8da4d948c2bf4
                         litex-ddr-arty-s7, vexriscv
The stale staged artifact designs/main/blinky-digilent-arty/blinky.bit was
removed from the COPY so no run can satisfy a target from staging leftovers.

openXC7.mk: bench-vivado did not stage it (the Vivado row never needed it).
Fetched on the dev mac from raw.githubusercontent.com at the exact frozen
demo-projects revisions and placed in the copies:
    designs/main/openXC7.mk      from 547ffc59  md5 c53a39fa54308cffca99fb6b4bffd934 (73 lines)
    designs/deephier/openXC7.mk  from f08d9e7b  md5 e52dd7bb178407a9d329c8251f4b9499 (61 lines)
The two differ only by main's .DELETE_ON_ERROR guard comment block.

The 4 frozen designs, as the harness runs them
----------------------------------------------
    blinky                      designs/main/blinky-digilent-arty   make blinky.{json,fasm,bit}
    picosoc-kx2                 designs/main/picosoc                make BOARD=kx2 picosoc.{json,fasm,bit}  (PART xc7k160tffg676-2)
    litex-ddr-arty-s7           designs/main/litex-ddr-arty-s7      make digilent_arty_s7.{json,fasm,bit}
    litex-ddr-arty-s7-deephier  designs/deephier/litex-ddr-arty-s7  make digilent_arty_s7.{json,fasm,bit}
Seed: each design's default (PNR_ARGS untouched, no --seed injected).
Env the harness exports (openXC7.mk's snap-layout defaults do not fit a
source tree): NEXTPNR_XILINX_DIR=/bench/src/nextpnr-xilinx,
NEXTPNR_XILINX_PYTHON_DIR=$NEXTPNR_XILINX_DIR/xilinx/python,
PRJXRAY_DB_DIR=$NEXTPNR_XILINX_DIR/xilinx/external/prjxray-db,
{ARTIX7,KINTEX7,SPARTAN7}_CHIPDB=/bench/chipdb.

Provisioning outcome (2026-08-15, all under fuzzer load -- untimed steps only)
------------------------------------------------------------------------------
- In-image build: OK (logs/build-nextpnr.log, logs/build-nextpnr-verify.log).
  ./nextpnr-xilinx reports "Version 0.9.2-98-g05aaa06b"; bbasm at repo root
  (bba/bba.cmake places it there, not under bba/).
- chipdb: OK (logs/gen-chipdb.log) -- xc7a35tcsg324.bin 92996364 B,
  xc7k160tffg676.bin 262011528 B, xc7s50csga324.bin 92437008 B.
- Harness smoke `./bench-openxc7.sh -n 1 blinky`: OK end to end
  (results/bench-openxc7-20260815-073143.csv). Produced a 2192116-byte
  xc7a35t bitstream; revisions in the CSV row were read from the tree.
  Smoke numbers are NOT benchmark numbers (load ~4-5 at the time; the
  harness printed its not-quiet warning as designed).
- The timed N=5 campaign has NOT been run (box must be quiet). Launch later:
    cd ~/data/openxc7/bench-openxc7 && \
      nohup ./bench-openxc7.sh -n 5 > logs/campaign-$(date +%Y%m%d-%H%M%S).log 2>&1 &
