# State of the tree after thirty-four iterations

Verified rather than described: every figure below was produced by running the
thing, not by reading a note about it.

## Gates: three, all green

| gate | result |
|---|---|
| `tools/check_script_rot.py` | OK -- every instantiated module exists in the files its script reads |
| `tools/check_harness.py` | OK -- no new partial-observation harnesses (83 baselined) |
| `conformance/check_artefact_agreement.py` | OK -- no new disagreements (13 baselined) |

All three are ratchets: known entries baselined, failure on new ones and on
baseline entries that stop reproducing.

## Paper

- Builds with **zero errors**, **zero dangling cross-references**
- **64 theorems, corollaries and lemmas** across 23 sections
- arXiv metadata abstract at **1920 of 1920** characters
- Publication **still blocked** by the owner's stop rule, which was measured
  against and went against us on the block axis

## Repositories

| repo | branch | state |
|---|---|---|
| `trinity-fpga` | `feat/ternary-network-angle` | **pushed**, 57 commits, working tree clean of our changes |
| `t27` | `feat/rename-tef-to-tnf` | clean |
| `trinity-s3ai` | `proofs/golden-alphabet-signed` | PR #50 open, 5 green / 3 red |
| `claim-audit-lab` | `case/tnf-2026-08-10` | PR #7 open, 2 green / 2 red |

Three files in `trinity-fpga` are modified and **were left alone**:
`apps/website/package-lock.json` and `src/main.tsx`, last touched by the owner
four and five months ago, and the `external/zig-golden-float` submodule. None is
ours to commit.

## The two open pull requests, and why they are red

**`trinity-s3ai` #50** -- six Coq theorems about the golden alphabet, kernel-checked
with a negative control. `build` passes. Red: a commit-signature gate that cannot
verify SSH signatures without an allowed-signers file in CI, and a Cyrillic check
that **also fails on `main`** for a file this PR does not touch. Neither is fixable
from our side: the first needs a GPG key, which is the owner's secret.

**`claim-audit-lab` #7** -- CASE-10 applying the five-label framework to our own
programme. `scan` passes. Red: `ascii-only` and `symmetric-mirror` fail on
`CASE-11` through `CASE-17`, seven files that **also fail on `main`** and that this
PR does not touch. A separate task was spawned for them.

## What is durable and what dies with the session

**Durable:** everything in the four repositories, three gates in CI, two PRs, the
method note, the selection table, and the skill file at
`~/.claude/skills/fpga-income/SKILL.md`, which carries the night's findings in a
form the next session reads first.

**Dies with the session:** the 15-minute cron job `c81e11ba`, which is
session-only by construction.
