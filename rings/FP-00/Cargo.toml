[package]
name = "trios-fpga-fp00"
version = "0.1.0"
edition = "2021"
description = "FP-00: HDL core types and board constants for trios-fpga"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
