name: unknown
version: "1.0.0"
language: zig
module: unknown

types:
  User:
    fields:
      id: Int
      name: String

  create_user:

behaviors:
  - name: create_user
    given: A name string
    when: create_user is called
    then: Returns a User with generated ID
  - name: creates user
