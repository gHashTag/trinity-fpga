# TRI-27 Research Plan — emit_t27 Implementation

**Date**: 2026-04-02

## План: emit_t27 (tri → t27 → VM pipeline)

### Зачем
- Разблокировать `.tri` спецификации → `.t27` ассемблерный код
- Включить полную систему кодогенерации для `.tri`

### Компоненты

| Компонент | Статус | Описание |
|---------|-------|---------|
| AST Parser | Существует | `src/tri27/vibee/parser.zig` |
| Code Generator | Существует | `src/tri27/vibee/emit_t27.zig` (только создан) |
| VM | Существует | `src/tri27/vm.zig` (но не VIBEE VM) |
| Opcodes | Существуют | Определены в .tri спецификациях |

### Этап 1: Parser
- [ ] Читить `src/tri27/vibee/parser.zig` в emit_t27
- [ ] Создать тривера в AST формате для каждого опкода
- [ ] Использовать существующие типы (Constant, Field, TestCase)
- [ ] Поддерживать все 257+ опкодов VSA

### Этап 2: Code Generator
- [ ] Расширить `emit_t27.zig` для генерации Zig кода из AST
- [ ] Поддерживать все типы и паттерны
- [ ] Сгенерировать `main.zig` как точку входа

### Этап 3: Включить emit_t27 в `tri gen`
- [ ] Добавить подкоманду `--emit` или `--emit-t27`
- [ ] Обеспечить что `tri gen` вызывает emit_t27

### Этап 4: Создать тесты
- [ ] Для каждого типа: `test_emit_t27_<type>.zig`
- [ ] Убедить что emit_t27 работает для всех типов

### Этап 5: Обновить документацию
- [ ] `src/tri27/vibee/` — существующее описание
- [ ] Добавить `specs/algo/` — формат .tri спецификаций

### Дедлайн
1 неделя (до 12 апреля)
- [ ] emit_t27 в build.zig
- [ ] emit_t27 генерация Zig кода
- [ ] базовые тесты (dense.tri, relu.tri)
- [ ] интеграция с `tri gen --emit`

---

## Текущее состояние

- Parser: ✓ Существует
- emit_t27: ✓ Создан
- Build: ✓ В build.zig
- Tests: ✅ Создан

## Следующие действия

1. Добавить `.tri → .t27` генерацию в `tri gen`
2. Создать базовые тесты для emit_t27
3. Обновить документацию

---
