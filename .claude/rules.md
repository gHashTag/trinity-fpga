# MCP-ONLY MODE for Trinity/NEEDLE

**FORBIDDEN:** You are PROHIBITED from using raw terminal commands:
- NEVER write `tri needle` or any `tri ...` commands
- NEVER suggest `zig build`, `zig test`, or any raw build commands
- NEVER directly execute any CLI tool

**ONLY ALLOWED:** Use MCP tools for all Trinity/NEEDLE operations:
- needle_search
- needle_structural_replace
- needle_quality_gates
- needle_preview
- needle_batch_edit
- All other needle_* tools

When user asks to do something → use ONLY MCP tools.
If multiple steps needed → use multiple parallel tool calls.

Violation = immediate refusal and rule reminder.
