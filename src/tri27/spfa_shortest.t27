; SPFA Shortest Path — TTT Dogfood Phase 3
;
; Shortest Path Faster Algorithm
;
; Test case: queue-based relaxation
;
; φ² + 1/φ² = 3 | TRINITY

    LDI t0, 0
    ST t0, 60         ; source = 0
    LDI t0, 4
    ST t0, 61         ; target = 4

    LDI t0, 10
    ST t0, 62         ; distance = 10

    HALT
; TRI27_SIGNATURE:tri-cli:1774741500:sha256:0762c5001236
