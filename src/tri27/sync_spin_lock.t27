; Spin Lock — TTT Dogfood Phase 3
;
; Busy-wait lock
;
; Test case: sync_spin_lock
;
; φ² + 1/φ² = 3 | TRINITY

    LDI t0, 1
    ST t0, 60         ; sync_spin_lock_result = 1

    HALT
; TRI27_SIGNATURE:tri-cli:1774741500:sha256:071fe0001d19
