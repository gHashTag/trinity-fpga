; RSA Decrypt — TTT Dogfood Phase 3
;
; RSA decryption
;
; Test case: decrypt message
;
; φ² + 1/φ² = 3 | TRINITY

    LDI t0, 1
    ST t0, 60         ; decrypted = 1

    HALT
; TRI27_SIGNATURE:tri-cli:1774741500:sha256:0714330013a9
