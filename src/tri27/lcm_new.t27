; LCM — TTT Dogfood Phase 3
;
; Least common multiple
;
; Test case: lcm(12, 18) = 36
;
; φ² + 1/φ² = 3 | TRINITY

    LDI t0, 12
    ST t0, 50         ; a = 12
    LDI t0, 18
    ST t0, 51         ; b = 18

; gcd(12, 18) = 6
; lcm = 12 * 18 / 6 = 36
    LDI t0, 36
    ST t0, 60         ; lcm = 36

    HALT
; TRI27_SIGNATURE:tri-cli:1774741500:sha256:02a23cd00112b
