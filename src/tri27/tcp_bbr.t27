; TCP BBR — TTT Dogfood Phase 3
;
; TCP BBR congestion control
;
; Test case: update rate
;
; φ² + 1/φ² = 3 | TRINITY

    LDI t0, 1
    ST t0, 60         ; rate = 1

    HALT
; TRI27_SIGNATURE:tri-cli:1774741500:sha256:07142f0013a5
