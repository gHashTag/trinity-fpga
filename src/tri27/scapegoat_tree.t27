; scapegoat Tree — TTT Dogfood Phase 3
;
; Rebalancing when height > log n
;
; Test case: insert triggers rebalance
;
; φ² + 1/φ² = 3 | TRINITY

    LDI t0, 5
    ST t0, 100        ; root = 5
    LDI t0, 4
    ST t0, 101        ; left = 4
    LDI t0, 3
    ST t0, 102        ; left->left = 3

    LDI t0, 1
    ST t0, 60         ; needs_rebalance = 1

    HALT
; TRI27_SIGNATURE:tri-cli:1774741500:sha256:0712a0001211
