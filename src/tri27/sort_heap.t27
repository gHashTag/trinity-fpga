; Heap Sort — TTT Dogfood Phase 3
;
; O(n log n) in-place sort
;
; Test case: sort_heap
;
; φ² + 1/φ² = 3 | TRINITY

    LDI t0, 1
    ST t0, 60         ; heap_sort_result = 1

    HALT
; TRI27_SIGNATURE:tri-cli:1774741500:sha256:079790000020000000021af
