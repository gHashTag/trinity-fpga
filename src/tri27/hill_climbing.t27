; Hill Climbing — TTT Dogfood Phase 3
;
; Local search: always move uphill
;
; Test case: find local maximum
;
; φ² + 1/φ² = 3 | TRINITY

    LDI t0, 50
    ST t0, 60         ; current = 50

    LDI t0, 55
    ST t0, 61         ; neighbor = 55

    LDI t0, 1
    ST t0, 70         ; moved = 1

    LDI t0, 55
    ST t0, 62         ; new_current = 55

    HALT
; TRI27_SIGNATURE:tri-cli:1774741500:sha256:07621d00118e
