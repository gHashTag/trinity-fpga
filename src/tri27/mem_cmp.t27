; memcmp — Compare Memory — TTT Dogfood Phase 3
;
; Compare two memory buffers
;
; Test case: buffers are equal
;
; φ² + 1/φ² = 3 | TRINITY

    LDI t0, 0
    ST t0, 60         ; result = 0 (equal)

    HALT
; TRI27_SIGNATURE:tri-cli:1774741500:sha256:0712df022557
