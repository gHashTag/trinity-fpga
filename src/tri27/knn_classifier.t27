; K-Nearest Neighbors — TTT Dogfood Phase 3
;
; Instance-based learning
;
; Test case: k=5, classify new point
;
; φ² + 1/φ² = 3 | TRINITY

    LDI t0, 5
    ST t0, 60         ; k = 5 neighbors

    LDI t0, 1
    ST t0, 61         ; class = 1

    HALT
; TRI27_SIGNATURE:tri-cli:1774741500:sha256:0712f3001269
