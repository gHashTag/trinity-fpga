; Thread Exit — TTT Dogfood Phase 3
;
; Terminate current thread
;
; Test case: sync_thread_exit
;
; φ² + 1/φ² = 3 | TRINITY

    LDI t0, 1
    ST t0, 60         ; sync_thread_exit_result = 1

    HALT
; TRI27_SIGNATURE:tri-cli:1774741500:sha256:0720b0001de9
