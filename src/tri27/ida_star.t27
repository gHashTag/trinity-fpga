; IDA* Search — TTT Dogfood Phase 3
;
; Iterative deepening A*
;
; Test case: depth-limited search
;
; φ² + 1/φ² = 3 | TRINITY

    LDI t0, 10
    ST t0, 60         ; threshold = 10

    LDI t0, 8
    ST t0, 61         ; min_cost = 8

    LDI t0, 1
    ST t0, 70         ; found = 1

    HALT
; TRI27_SIGNATURE:tri-cli:1774741500:sha256:072211001182
