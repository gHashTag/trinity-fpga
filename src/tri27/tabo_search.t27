; Tabu Search — TTT Dogfood Phase 3
;
; Metaheuristic with tabu list
;
; Test case: avoid revisiting solutions
;
; φ² + 1/φ² = 3 | TRINITY

    LDI t0, 100
    ST t0, 60         ; current_cost = 100

    LDI t0, 5
    ST t0, 61         ; tabu_list_size = 5

    LDI t0, 80
    ST t0, 62         ; best_cost = 80

    LDI t0, 1
    ST t0, 70         ; improved = 1

    HALT
; TRI27_SIGNATURE:tri-cli:1774741500:sha256:071218001189
