; Beam Search — TTT Dogfood Phase 3
;
; Width-limited best-first search
;
; Test case: beam width = 2
;
; φ² + 1/φ² = 3 | TRINITY

    LDI t0, 2
    ST t0, 50         ; beam_width = 2

    LDI t0, 10
    ST t0, 100        ; node 1: score=10
    LDI t0, 8
    ST t0, 101        ; node 2: score=8 (kept)
    LDI t0, 5
    ST t0, 102        ; node 3: score=5 (pruned)

    LDI t0, 2
    ST t0, 60         ; kept = 2

    HALT
; TRI27_SIGNATURE:tri-cli:1774741500:sha256:071210001181
