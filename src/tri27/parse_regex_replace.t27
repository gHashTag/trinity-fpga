; Regex Replace — TTT Dogfood Phase 3
;
; Replace regex matches
;
; Test case: parse_regex_replace
;
; φ² + 1/φ² = 3 | TRINITY

    LDI t0, 1
    ST t0, 60         ; parse_regex_replace_result = 1

    HALT
; TRI27_SIGNATURE:tri-cli:1774741500:sha256:072160001d99
