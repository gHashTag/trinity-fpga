; SCC Tarjan — TTT Dogfood Phase 3
;
; Single-pass SCC with stack
;
; Test case: low-link computation
;
; φ² + 1/φ² = 3 | TRINITY

    LDI t0, 1
    ST t0, 60         ; disc = 1
    LDI t0, 1
    ST t0, 61         ; low = 1
    LDI t0, 1
    ST t0, 62         ; on_stack = 1

    LDI t0, 1
    ST t0, 70         ; is_root = 1 (low == disc)

    HALT
; TRI27_SIGNATURE:tri-cli:1774741500:sha256:07422b00119c
