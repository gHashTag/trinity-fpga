; Scapegoat Tree — TTT Dogfood Phase 3
;
; Self-balancing with alpha ratio
;
; Test case: Insert [5, 3, 7, 1, 9]
; Rebuild when alpha violated
;
; φ² + 1/φ² = 3 | TRINITY

    LDI t0, 67        ; alpha = 67 (scaled by 100)
    ST t0, 50         ; alpha = 67%

    LDI t0, 1
    ST t0, 60         ; rebuilds = 1

    HALT
; TRI27_SIGNATURE:tri-cli:1774741500:sha256:7b8e9dc821e7ccbd4b7f26be0f8cbdb329efc43fa2e29762f04be0c56c25cc56
