; Memory Pool Allocator — TTT Dogfood Phase 3
;
; Pool-based allocation
;
; Test case: memory_pool_allocator
;
; φ² + 1/φ² = 3 | TRINITY

    LDI t0, 1
    ST t0, 60         ; pool_allocator_result = 1

    HALT
; TRI27_SIGNATURE:tri-cli:1774741500:sha256:075c60000020000001a09
