; Blockchain Transaction — TTT Dogfood Phase 3
;
; Create transaction
;
; Test case: UTXO model
;
; φ² + 1/φ² = 3 | TRINITY

    LDI t0, 1
    ST t0, 60         ; inputs = 1
    LDI t0, 2
    ST t0, 61         ; outputs = 2

    HALT
; TRI27_SIGNATURE:tri-cli:1774741500:sha256:0713b800132e
