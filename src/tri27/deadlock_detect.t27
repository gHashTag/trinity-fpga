; Deadlock Detection — TTT Dogfood Phase 3
;
; Wait-for graph cycle detection
;
; Test case: cycle found
;
; φ² + 1/φ² = 3 | TRINITY

    LDI t0, 1
    ST t0, 60         ; deadlock = 1

    HALT
; TRI27_SIGNATURE:tri-cli:1774741500:sha256:0713560012cc
