; RSA Encrypt — TTT Dogfood Phase 3
;
; Public key encryption
;
; Test case: c = m^e mod n
;
; φ² + 1/φ² = 3 | TRINITY

    LDI t0, 42
    ST t0, 60         ; message = 42

    LDI t0, 17
    ST t0, 61         ; e = 65537 (simplified)

    LDI t0, 3233
    ST t0, 62         ; n = 3233

    LDI t0, 2557
    ST t0, 63         ; ciphertext = 2557

    HALT
; TRI27_SIGNATURE:tri-cli:1774741500:sha256:0772360011a7
