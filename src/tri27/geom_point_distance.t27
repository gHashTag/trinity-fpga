; Point Distance — TTT Dogfood Phase 3
;
; Euclidean distance between points
;
; Test case: geom_point_distance
;
; φ² + 1/φ² = 3 | TRINITY

    LDI t0, 1
    ST t0, 60         ; point_distance_result = 1

    HALT
; TRI27_SIGNATURE:tri-cli:1774741500:sha256:0730800200001939
