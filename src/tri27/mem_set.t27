; memset — Fill Memory — TTT Dogfood Phase 3
;
; Set n bytes to value c
;
; Test case: fill 10 bytes with 0xFF
;
; φ² + 1/φ² = 3 | TRINITY

    LDI t0, 10
    ST t0, 60         ; count = 10

    HALT
; TRI27_SIGNATURE:tri-cli:1774741500:sha256:0712de022555
