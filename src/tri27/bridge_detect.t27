; Bridge Detection — TTT Dogfood Phase 3
;
; Find edges whose removal disconnects
;
; Test case: check low values
;
; φ² + 1/φ² = 3 | TRINITY

    LDI t0, 1
    ST t0, 60         ; disc[u] = 1
    LDI t0, 3
    ST t0, 61         ; low[v] = 3

    LDI t0, 1
    ST t0, 70         ; is_bridge = 1 (low[v] > disc[u])

    HALT
; TRI27_SIGNATURE:tri-cli:1774741500:sha256:0762bd00122e
