; Selection Sort — TTT Dogfood Phase 3
;
; O(n^2) with minimal writes
;
; Test case: sort_selection
;
; φ² + 1/φ² = 3 | TRINITY

    LDI t0, 1
    ST t0, 60         ; selection_result = 1

    HALT
; TRI27_SIGNATURE:tri-cli:1774741500:sha256:0797d0000020000000021b1
