; Softmax Function — TTT Dogfood Phase 3
;
; exp(x)/sum(exp(x))
;
; Test case: 3 classes
;
; φ² + 1/φ² = 3 | TRINITY

    LDI t0, 3
    ST t0, 60         ; n = 3 classes

    HALT
; TRI27_SIGNATURE:tri-cli:1774741500:sha256:0712ff001275
