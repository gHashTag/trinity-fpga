; Dinic Algorithm — TTT Dogfood Phase 3
;
; Max flow with level graph
;
; Test case: BFS level graph then DFS
;
; φ² + 1/φ² = 3 | TRINITY

    LDI t0, 0
    ST t0, 60         ; source = 0
    LDI t0, 6
    ST t0, 61         ; sink = 6

    LDI t0, 20
    ST t0, 62         ; max_flow = 20

    LDI t0, 3
    ST t0, 63         ; num_phases = 3

    HALT
; TRI27_SIGNATURE:tri-cli:1774741500:sha256:0772b6001227
