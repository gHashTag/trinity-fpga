; Bootstrap Resampling — TTT Dogfood Phase 3
;
; Resampling with replacement
;
; Test case: boot
;
; φ² + 1/φ² = 3 | TRINITY

    LDI t0, 1
    ST t0, 60         ; boot_result = 1

    HALT
; TRI27_SIGNATURE:tri-cli:1774741500:sha256:071abe001a36
