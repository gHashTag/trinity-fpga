; Linear Regression — TTT Dogfood Phase 3
;
; Ordinary least squares regression
;
; Test case: y = mx + b fitting
;
; φ² + 1/φ² = 3 | TRINITY

    LDI t0, 5
    ST t0, 60         ; n = 5 points

    LDI t0, 2
    ST t0, 61         ; slope = 2.0
    LDI t0, 1
    ST t0, 62         ; intercept = 1.0

    HALT
; TRI27_SIGNATURE:tri-cli:1774741500:sha256:0712f0001266
