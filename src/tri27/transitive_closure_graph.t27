; Transitive Closure Graph — TTT Dogfood Phase 3
;
; Reachability matrix for all pairs
;
; Test case: Floyd-Warshall variant
;
; φ² + 1/φ² = 3 | TRINITY

    LDI t0, 3
    ST t0, 60         ; n = 3

    LDI t0, 1
    ST t0, 100        ; reach[0][1] = 1
    LDI t0, 1
    ST t0, 101        ; reach[1][2] = 1
    LDI t0, 1
    ST t0, 102        ; reach[0][2] = 1 (derived)

    LDI t0, 6
    ST t0, 61         ; num_pairs = 6

    HALT
; TRI27_SIGNATURE:tri-cli:1774741500:sha256:0772be00122f
