; B+ Tree Delete — TTT Dogfood Phase 3
;
; Delete from B+ tree
;
; Test case: delete 10, borrow from sibling
;
; φ² + 1/φ² = 3 | TRINITY

    LDI t0, 10
    ST t0, 60         ; key = 10

    LDI t0, 5
    ST t0, 100        ; leaf[0] = 5
    LDI t0, 15
    ST t0, 101        ; leaf[1] = 15 (10 deleted)

    LDI t0, 2
    ST t0, 61         ; num_keys = 2

    HALT
; TRI27_SIGNATURE:tri-cli:1774741500:sha256:0722a900121a
