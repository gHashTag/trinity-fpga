; SVD — Singular Value Decomposition — TTT Dogfood Phase 3
;
; Factorize matrix into UΣV^T
;
; Test case: 2x2 matrix
; Σ = [s1, s2]
;
; φ² + 1/φ² = 3 | TRINITY

    LDI t0, 2
    ST t0, 60         ; rank = 2

; Singular values
    LDI t0, 5
    ST t0, 61         ; sigma[0] = 5
    LDI t0, 2
    ST t0, 62         ; sigma[1] = 2

    HALT
; TRI27_SIGNATURE:tri-cli:1774741500:sha256:0452d1001140
