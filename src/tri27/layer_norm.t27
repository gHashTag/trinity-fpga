; Layer Normalization — TTT Dogfood Phase 3
;
; Normalize across features
;
; Test case: normalize layer
;
; φ² + 1/φ² = 3 | TRINITY

    LDI t0, 1
    ST t0, 60         ; normalized = 1

    HALT
; TRI27_SIGNATURE:tri-cli:1774741500:sha256:0713f400136a
