; Arithmetic Decode — TTT Dogfood Phase 3
;
; Decode using arithmetic coding
;
; Test case: decode value 0.5
;
; φ² + 1/φ² = 3 | TRINITY

    LDI t0, 50        ; 0.5 * 100
    ST t0, 60         ; value = 0.5

    LDI t0, 65        ; A
    ST t0, 61         ; symbol = A

    HALT
; TRI27_SIGNATURE:tri-cli:1774741500:sha256:0772860011f7
