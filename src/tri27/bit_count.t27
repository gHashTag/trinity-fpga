; Bit Count — TTT Dogfood Phase 3
;
; Count set bits (population count)
;
; Test case: popcount(1011) = 3
;
; φ² + 1/φ² = 3 | TRINITY

    LDI t0, 11        ; 1011
    ST t0, 60         ; n = 11

    LDI t0, 3
    ST t0, 61         ; count = 3

    HALT
; TRI27_SIGNATURE:tri-cli:1774741500:sha256:07427b0011ec
