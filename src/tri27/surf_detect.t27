; SURF Detect — TTT Dogfood Phase 3
;
; SURF keypoint detection
;
; Test case: surf keypoints
;
; φ² + 1/φ² = 3 | TRINITY

    LDI t0, 1
    ST t0, 60         ; keypoints = 1

    HALT
; TRI27_SIGNATURE:tri-cli:1774741500:sha256:0715650014db
