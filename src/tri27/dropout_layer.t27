; Dropout Layer — TTT Dogfood Phase 3
;
; Dropout regularization
;
; Test case: dropout_layer
;
; φ² + 1/φ² = 3 | TRINITY

    LDI t0, 1
    ST t0, 60         ; dropout_layer_result = 1

    HALT
; TRI27_SIGNATURE:tri-cli:1774741500:sha256:071ade001a56
