; LSD Matching — TTT Dogfood Phase 3
;
; Line segment detector matching
;
; Test case: lsd matching
;
; φ² + 1/φ² = 3 | TRINITY

    LDI t0, 1
    ST t0, 60         ; matched = 1

    HALT
; TRI27_SIGNATURE:tri-cli:1774741500:sha256:071791001707