; SCC Kosaraju — TTT Dogfood Phase 3
;
; Strongly connected components via 2-pass DFS
;
; Test case: transpose graph then DFS
;
; φ² + 1/φ² = 3 | TRINITY

    LDI t0, 3
    ST t0, 60         ; num_scc = 3

    LDI t0, 1
    ST t0, 100        ; scc_id[0] = 1
    LDI t0, 1
    ST t0, 101        ; scc_id[1] = 1
    LDI t0, 2
    ST t0, 102        ; scc_id[2] = 2
    LDI t0, 3
    ST t0, 103        ; scc_id[3] = 3

    HALT
; TRI27_SIGNATURE:tri-cli:1774741500:sha256:07322a00119b
