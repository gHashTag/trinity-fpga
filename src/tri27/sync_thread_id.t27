; Thread ID — TTT Dogfood Phase 3
;
; Get current thread identifier
;
; Test case: sync_thread_id
;
; φ² + 1/φ² = 3 | TRINITY

    LDI t0, 1
    ST t0, 60         ; sync_thread_id_result = 1

    HALT
; TRI27_SIGNATURE:tri-cli:1774741500:sha256:0720e0001d19
