; Nearest Neighbor TSP — TTT Dogfood Phase 3
;
; Greedy TSP approximation
;
; Test case: always visit nearest unvisited
;
; φ² + 1/φ² = 3 | TRINITY

    LDI t0, 5
    ST t0, 60         ; num_cities = 5
    LDI t0, 0
    ST t0, 61         ; start = 0

    LDI t0, 50
    ST t0, 62         ; tour_length = 50

    HALT
; TRI27_SIGNATURE:tri-cli:1774741500:sha256:0712d0001241
