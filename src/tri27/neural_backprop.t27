; Neural Backprop — TTT Dogfood Phase 3
;
; Backward propagation through neural network
;
; Test case: backward pass
;
; φ² + 1/φ² = 3 | TRINITY

    LDI t0, 1
    ST t0, 60         ; gradient = 1

    HALT
; TRI27_SIGNATURE:tri-cli:1774741500:sha256:0713ee001364
