; Transformer Block — TTT Dogfood Phase 3
;
; Self-attention + feed-forward
;
; Test case: 8 heads, 512 model dim
;
; φ² + 1/φ² = 3 | TRINITY

    LDI t0, 8
    ST t0, 60         ; n_heads = 8
    LDI t0, 512
    ST t0, 61         ; d_model = 512

    HALT
; TRI27_SIGNATURE:tri-cli:1774741500:sha256:07130700127d
