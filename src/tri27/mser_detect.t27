; MSER Detect — TTT Dogfood Phase 3
;
; MSER region detection
;
; Test case: mser regions
;
; φ² + 1/φ² = 3 | TRINITY

    LDI t0, 1
    ST t0, 60         ; regions = 1

    HALT
; TRI27_SIGNATURE:tri-cli:1774741500:sha256:0715690014df
