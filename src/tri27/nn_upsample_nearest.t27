; Upsample Nearest — TTT Dogfood Phase 3
;
; Nearest neighbor upsampling
;
; Test case: nn_upsample_nearest
;
; φ² + 1/φ² = 3 | TRINITY

    LDI t0, 1
    ST t0, 60         ; nn_upsample_nearest_result = 1

    HALT
; TRI27_SIGNATURE:tri-cli:1774741500:sha256:072d100000001d49
