; Bloom Filter — TTT Dogfood Phase 3
;
; Bloom filter data structure
;
; Test case: check membership
;
; φ² + 1/φ² = 3 | TRINITY

    LDI t0, 1
    ST t0, 60         ; maybe = 1

    HALT
; TRI27_SIGNATURE:tri-cli:1774741500:sha256:0714580013ce
