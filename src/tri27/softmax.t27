; Softmax — TTT Dogfood Phase 3
;
; Softmax normalization
;
; Test case: softmax output
;
; φ² + 1/φ² = 3 | TRINITY

    LDI t0, 1
    ST t0, 60         ; probability = 1.0

    HALT
; TRI27_SIGNATURE:tri-cli:1774741500:sha256:0713f2001368
