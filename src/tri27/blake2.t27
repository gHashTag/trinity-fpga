; BLAKE2 — TTT Dogfood Phase 3
;
; BLAKE2 hash algorithm
;
; Test case: blake2 hash
;
; φ² + 1/φ² = 3 | TRINITY

    LDI t0, 1
    ST t0, 60         ; blake2_hash = 1

    HALT
; TRI27_SIGNATURE:tri-cli:1774741500:sha256:07180f001785
