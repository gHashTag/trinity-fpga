; String strstr Naive — TTT Dogfood Phase 3
;
; Naive substring search
;
; Test case: string_strstr_naive
;
; φ² + 1/φ² = 3 | TRINITY

    LDI t0, 1
    ST t0, 60         ; strstr_naive_result = 1

    HALT
; TRI27_SIGNATURE:tri-cli:1774741500:sha256:073200000020000003d9
