; Linear Hash Search — TTT Dogfood Phase 3
;
; Search with linear probing
;
; Test case: find key 11
;
; φ² + 1/φ² = 3 | TRINITY

    LDI t0, 11
    ST t0, 60         ; key = 11

    LDI t0, 1
    ST t0, 100        ; table[0] = 1
    LDI t0, 11
    ST t0, 101        ; table[1] = 11
    LDI t0, 21
    ST t0, 102        ; table[2] = 21

    LDI t0, 1
    ST t0, 61         ; found = 1 (at index 1)

    HALT
; TRI27_SIGNATURE:tri-cli:1774741500:sha256:0742ab00121c
