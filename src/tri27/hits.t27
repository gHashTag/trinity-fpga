; HITS — Hubs and Authorities — TTT Dogfood Phase 3
;
; Link analysis algorithm
;
; Test case: 3 pages
; Hub scores: [0.7, 0.6, 0.3]
;
; φ² + 1/φ² = 3 | TRINITY

    LDI t0, 70
    ST t0, 60         ; hub[0] = 0.7
    LDI t0, 60
    ST t0, 61         ; hub[1] = 0.6
    LDI t0, 30
    ST t0, 62         ; hub[2] = 0.3

    HALT
; TRI27_SIGNATURE:tri-cli:1774741500:sha256:0502d6001145
