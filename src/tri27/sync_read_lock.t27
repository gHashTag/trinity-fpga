; Read Lock — TTT Dogfood Phase 3
;
; Acquire read lock (RWLock)
;
; Test case: sync_read_lock
;
; φ² + 1/φ² = 3 | TRINITY

    LDI t0, 1
    ST t0, 60         ; sync_read_lock_result = 1

    HALT
; TRI27_SIGNATURE:tri-cli:1774741500:sha256:071fb0001de9
