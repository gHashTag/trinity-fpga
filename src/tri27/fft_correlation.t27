; FFT Correlation — TTT Dogfood Phase 3
;
; FFT-based correlation
;
; Test case: fft_correlation
;
; φ² + 1/φ² = 3 | TRINITY

    LDI t0, 1
    ST t0, 60         ; fft_corr_result = 1

    HALT
; TRI27_SIGNATURE:tri-cli:1774741500:sha256:074f80000020000012c9
