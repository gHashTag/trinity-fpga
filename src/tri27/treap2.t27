; Treap — TTT Dogfood Phase 3
;
; Treap (tree + heap)
;
; Test case: treap insert
;
; φ² + 1/φ² = 3 | TRINITY

    LDI t0, 1
    ST t0, 60         ; inserted = 1

    HALT
; TRI27_SIGNATURE:tri-cli:1774741500:sha256:0714680013de
