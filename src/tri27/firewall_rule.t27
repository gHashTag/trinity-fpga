; Firewall Rule — TTT Dogfood Phase 3
;
; Packet filtering rule
;
; Test case: allow HTTP
;
; φ² + 1/φ² = 3 | TRINITY

    LDI t0, 1
    ST t0, 60         ; allowed = 1

    HALT
; TRI27_SIGNATURE:tri-cli:1774741500:sha256:0713d600134c
