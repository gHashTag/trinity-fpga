; Embedding Layer — TTT Dogfood Phase 3
;
; Word embedding lookup
;
; Test case: nn_embedding
;
; φ² + 1/φ² = 3 | TRINITY

    LDI t0, 1
    ST t0, 60         ; nn_embedding_result = 1

    HALT
; TRI27_SIGNATURE:tri-cli:1774741500:sha256:072bf0000001d29
