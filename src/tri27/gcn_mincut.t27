; GCN Mincut — TTT Dogfood Phase 3
;
; Mincut pooling for graph classification
;
; Test case: gcn_mincut
;
; φ² + 1/φ² = 3 | TRINITY

    LDI t0, 1
    ST t0, 60         ; mincut_result = 1

    HALT
; TRI27_SIGNATURE:tri-cli:1774741500:sha256:07af700000200000002548
