; Uniform Distribution — TTT Dogfood Phase 3
;
; Uniform float in [0, 1)
;
; Test case: rand_uniform
;
; φ² + 1/φ² = 3 | TRINITY

    LDI t0, 1
    ST t0, 60         ; rand_uniform_result = 1

    HALT
; TRI27_SIGNATURE:tri-cli:1774741500:sha256:0725800001db9
