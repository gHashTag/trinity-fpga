; Count-Min Sketch — TTT Dogfood Phase 3
;
; Frequency estimation
;
; Test case: width=1000, depth=5
;
; φ² + 1/φ² = 3 | TRINITY

    LDI t0, 1000
    ST t0, 60         ; width = 1000
    LDI t0, 5
    ST t0, 61         ; depth = 5

    HALT
; TRI27_SIGNATURE:tri-cli:1774741500:sha256:071320032296
