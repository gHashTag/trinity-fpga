; Local Beam Search — TTT Dogfood Phase 3
;
; Keep k best states at each level
;
; Test case: beam width = 3
;
; φ² + 1/φ² = 3 | TRINITY

    LDI t0, 3
    ST t0, 50         ; k = 3

    LDI t0, 10
    ST t0, 100        ; state 1: score=10
    LDI t0, 8
    ST t0, 101        ; state 2: score=8
    LDI t0, 6
    ST t0, 102        ; state 3: score=6

    LDI t0, 3
    ST t0, 60         ; kept = 3

    HALT
; TRI27_SIGNATURE:tri-cli:1774741500:sha256:07721e00118f
