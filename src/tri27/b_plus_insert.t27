; B+ Tree Insert — TTT Dogfood Phase 3
;
; Insert into B+ tree (leaf nodes only)
;
; Test case: insert 5, 10, 15, 20
;
; φ² + 1/φ² = 3 | TRINITY

    LDI t0, 3
    ST t0, 50         ; order = 3 (max 3 keys per node)

    LDI t0, 5
    ST t0, 100        ; leaf[0] = 5
    LDI t0, 10
    ST t0, 101        ; leaf[1] = 10
    LDI t0, 15
    ST t0, 102        ; leaf[2] = 15

    LDI t0, 3
    ST t0, 60         ; num_keys = 3

    HALT
; TRI27_SIGNATURE:tri-cli:1774741500:sha256:0772a6001217
