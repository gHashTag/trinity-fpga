; Random Choice — TTT Dogfood Phase 3
;
; Pick random element from array
;
; Test case: rand_choice
;
; φ² + 1/φ² = 3 | TRINITY

    LDI t0, 1
    ST t0, 60         ; rand_choice_result = 1

    HALT
; TRI27_SIGNATURE:tri-cli:1774741500:sha256:0725400001d79
