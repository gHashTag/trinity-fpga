; Taylor Series Ln — TTT Dogfood Phase 3
;
; ln(1+x) = x - x^2/2 + x^3/3 - ... for |x| < 1
;
; Test case: ln(2) ≈ 0.693
;
; φ² + 1/φ² = 3 | TRINITY

    LDI t0, 1
    ST t0, 60         ; x = 1 (ln(2) = ln(1+1))

    LDI t0, 69        ; 0.69 * 100
    ST t0, 61         ; result ≈ 0.69

    HALT
; TRI27_SIGNATURE:tri-cli:1774741500:sha256:0742630011d4
