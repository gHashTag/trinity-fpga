; Cycle Sorter — TTT Dogfood Phase 3
;
; Minimize write operations
;
; Test case: sort [3,1,2]
;
; φ² + 1/φ² = 3 | TRINITY

    LDI t0, 3
    ST t0, 100        ; arr[0] = 3
    LDI t0, 1
    ST t0, 101        ; arr[1] = 1
    LDI t0, 2
    ST t0, 102        ; arr[2] = 2

    LDI t0, 1
    ST t0, 103        ; arr[0] = 1
    LDI t0, 2
    ST t0, 104        ; arr[1] = 2
    LDI t0, 3
    ST t0, 105        ; arr[2] = 3

    HALT
; TRI27_SIGNATURE:tri-cli:1774741500:sha256:07229900120a
