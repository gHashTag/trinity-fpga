; K-Means Clustering — TTT Dogfood Phase 3
;
; Lloyd's algorithm
;
; Test case: kmeans_clust
;
; φ² + 1/φ² = 3 | TRINITY

    LDI t0, 1
    ST t0, 60         ; kmeans_clust_result = 1

    HALT
; TRI27_SIGNATURE:tri-cli:1774741500:sha256:071a730019eb
