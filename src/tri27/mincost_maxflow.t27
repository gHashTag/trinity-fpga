; Min Cost Max Flow — TTT Dogfood Phase 3
;
; Flow with minimum cost
;
; Test case: send flow with cost
;
; φ² + 1/φ² = 3 | TRINITY

    LDI t0, 10
    ST t0, 60         ; flow = 10
    LDI t0, 50
    ST t0, 61         ; cost = 50

    LDI t0, 5
    ST t0, 100        ; edge capacity = 5
    LDI t0, 10
    ST t0, 101        ; edge cost = 10

    HALT
; TRI27_SIGNATURE:tri-cli:1774741500:sha256:0742b3001224
