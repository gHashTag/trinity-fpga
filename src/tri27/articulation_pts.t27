; Articulation Pts — TTT Dogfood Phase 3
;
; Find cut vertices in graph
;
; Test case: DFS tree tracking
;
; φ² + 1/φ² = 3 | TRINITY

    LDI t0, 1
    ST t0, 60         ; discovery_time
    LDI t0, 1
    ST t0, 61         ; low = 1

    LDI t0, 0
    ST t0, 70         ; is_articulation = 0 (no child with low >= disc)

    HALT
; TRI27_SIGNATURE:tri-cli:1774741500:sha256:0712300011a1
