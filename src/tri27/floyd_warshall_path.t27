; Floyd Warshall Path — TTT Dogfood Phase 3
;
; Reconstruct path from distance matrix
;
; Test case: next[] matrix for path reconstruction
;
; φ² + 1/φ² = 3 | TRINITY

    LDI t0, 1
    ST t0, 100        ; dist[0][1] = 1
    LDI t0, 2
    ST t0, 101        ; dist[0][2] = 2

    LDI t0, 2
    ST t0, 110        ; next[0][1] = 2
    LDI t0, 3
    ST t0, 111        ; next[0][2] = 3

    HALT
; TRI27_SIGNATURE:tri-cli:1774741500:sha256:0702c7001238
