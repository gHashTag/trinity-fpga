; JSON Escape — TTT Dogfood Phase 3
;
; Escape JSON strings
;
; Test case: crypto_json_escape
;
; φ² + 1/φ² = 3 | TRINITY

    LDI t0, 1
    ST t0, 60         ; crypto_json_escape_result = 1

    HALT
; TRI27_SIGNATURE:tri-cli:1774741500:sha256:071c80001db9
