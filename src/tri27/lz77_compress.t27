; LZ77 Compression — TTT Dogfood Phase 3
;
; Sliding window compression
;
; Test case: compress "ABABAB"
;
; φ² + 1/φ² = 3 | TRINITY

    LDI t0, 6
    ST t0, 60         ; input_len = 6
    LDI t0, 4
    ST t0, 61         ; output_len = 4

    HALT
; TRI27_SIGNATURE:tri-cli:1774741500:sha256:07132400129a
