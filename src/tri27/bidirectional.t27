; Bidirectional Search — TTT Dogfood Phase 3
;
; Search from both ends
;
; Test case: meet in middle
;
; φ² + 1/φ² = 3 | TRINITY

    LDI t0, 0
    ST t0, 60         ; forward_depth = 3
    LDI t0, 3
    ST t0, 61         ; backward_depth = 2

    LDI t0, 5
    ST t0, 62         ; meeting_node = 5

    LDI t0, 1
    ST t0, 70         ; found = 1

    HALT
; TRI27_SIGNATURE:tri-cli:1774741500:sha256:073212001183
