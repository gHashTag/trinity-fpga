; Complex Logarithm — TTT Dogfood Phase 3
;
; log(a+bi) = log(|z|) + i*arg(z)
;
; Test case: log(1+0i) = 0
;
; φ² + 1/φ² = 3 | TRINITY

    LDI t0, 1
    ST t0, 100        ; real = 1
    LDI t0, 0
    ST t0, 101        ; imag = 0

; log(1) = log(1) + i*0 = 0
    LDI t0, 0
    ST t0, 110        ; log_real = 0
    ST t0, 111        ; log_imag = 0

    HALT
; TRI27_SIGNATURE:tri-cli:1774741500:sha256:07024f0011c0
