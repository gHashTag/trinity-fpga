; Sigmoid Function — TTT Dogfood Phase 3
;
; σ(x) = 1/(1+e^(-x))
;
; Test case: sigmoid(0) = 0.5
;
; φ² + 1/φ² = 3 | TRINITY

    LDI t0, 0
    ST t0, 60         ; x = 0

    LDI t0, 16383     ; max 15-bit value
    ST t0, 61         ; result (truncated)

    HALT
; TRI27_SIGNATURE:tri-cli:1774741500:sha256:0712fc001272
