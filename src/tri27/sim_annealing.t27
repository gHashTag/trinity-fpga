; Simulated Annealing Search — TTT Dogfood Phase 3
;
; Probabilistic acceptance of worse solutions
;
; Test case: temperature-based search
;
; φ² + 1/φ² = 3 | TRINITY

    LDI t0, 1000
    ST t0, 60         ; temp = 1000

    LDI t0, 95
    ST t0, 61         ; cooling_rate = 0.95

    LDI t0, 950
    ST t0, 62         ; new_temp = 950

    LDI t0, 1
    ST t0, 70         ; accepted = 1

    HALT
; TRI27_SIGNATURE:tri-cli:1774741500:sha256:07021f001190
