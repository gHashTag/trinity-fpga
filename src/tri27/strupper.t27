; strupper — To Uppercase — TTT Dogfood Phase 3
;
; Convert string to uppercase
;
; Test case: "Hello" → "HELLO"
;
; φ² + 1/φ² = 3 | TRINITY

    LDI t0, 5
    ST t0, 60         ; length = 5

    HALT
; TRI27_SIGNATURE:tri-cli:1774741500:sha256:0712da001251
