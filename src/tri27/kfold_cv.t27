; K-Fold CV — TTT Dogfood Phase 3
;
; K-fold cross validation
;
; Test case: kfold_cv
;
; φ² + 1/φ² = 3 | TRINITY

    LDI t0, 1
    ST t0, 60         ; kfold_cv_result = 1

    HALT
; TRI27_SIGNATURE:tri-cli:1774741500:sha256:071ac1001a39
