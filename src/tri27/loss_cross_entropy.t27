; Cross Entropy Loss — TTT Dogfood Phase 3
;
; Categorical cross entropy
;
; Test case: loss_cross_entropy
;
; φ² + 1/φ² = 3 | TRINITY

    LDI t0, 1
    ST t0, 60         ; loss_cross_entropy_result = 1

    HALT
; TRI27_SIGNATURE:tri-cli:1774741500:sha256:072b60000001d99
