; memmove — Move Memory — TTT Dogfood Phase 3
;
; Copy n bytes handling overlap
;
; Test case: move 16 bytes safely
;
; φ² + 1/φ² = 3 | TRINITY

    LDI t0, 16
    ST t0, 60         ; bytes_moved = 16

    HALT
; TRI27_SIGNATURE:tri-cli:1774741500:sha256:0712e0022558
