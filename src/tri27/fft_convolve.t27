; FFT Convolution — TTT Dogfood Phase 3
;
; Fast convolution via FFT
;
; Test case: fft_convolve
;
; φ² + 1/φ² = 3 | TRINITY

    LDI t0, 1
    ST t0, 60         ; fft_convolve_result = 1

    HALT
; TRI27_SIGNATURE:tri-cli:1774741500:sha256:071a1b001993
