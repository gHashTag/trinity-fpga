; WAVL Tree — TTT Dogfood Phase 3
;
; Weight-balanced tree with rank
;
; Test case: rank-based operations
;
; φ² + 1/φ² = 3 | TRINITY

    LDI t0, 5
    ST t0, 100        ; root = 5
    LDI t0, 2
    ST t0, 101        ; rank = 2

    LDI t0, 3
    ST t0, 110        ; left = 3
    LDI t0, 1
    ST t0, 111        ; left_rank = 1

    HALT
; TRI27_SIGNATURE:tri-cli:1774741500:sha256:0742a3001214
