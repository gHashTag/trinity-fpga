// =============================================================================
// FORGE OF KOSCHEI — segbits_data.zig (stub)
// =============================================================================
//
// Auto-generated stub for CI compilation.
// Real data is generated by: python3 tools/gen_segbits.py --part xc7a100t
// The generated file is in .gitignore — this stub provides the API contract
// so that dependent code compiles without the full dataset.
//
// =============================================================================

/// A single configuration bit mapping within a tile.
pub const SegBit = struct {
    frame_offset: u8,
    bit_index: u8,
    inverted: bool = false,
};

/// A feature entry: feature name → array of config bits.
pub const FeatureEntry = struct {
    feature: []const u8,
    bits: []const SegBit,
};

/// A tile instance from the tilegrid (e.g., "CLBLL_L_X2Y148").
pub const TileInstance = struct {
    name: []const u8,
    baseaddr: u32,
    offset: u16,
};

/// Statistics about the generated dataset.
pub const total_features: usize = 0;
pub const total_bits: usize = 0;
pub const tile_type_count: usize = 0;

/// Look up all feature entries for a given tile type.
pub fn getFeaturesForTileType(_: []const u8) ?[]const FeatureEntry {
    return null;
}

/// Find a tile instance by tile type and instance name.
pub fn findTileInstance(_: []const u8, _: []const u8) ?*const TileInstance {
    return null;
}
