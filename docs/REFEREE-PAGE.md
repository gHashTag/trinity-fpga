# One page for a referee — claim, record, limitation

Every row is a claim this project currently makes, the committed record that
supports it, and the limitation that bounds it. Nothing here is a claim from the
manuscript; these are the measurements produced by the autonomous audit
(W936–W942) and landed upstream as tf#634, #636, #638, #640, #641, #642, #643.

| # | claim | record | limitation |
|---|---|---|---|
| 1 | **WITHDRAWN (W946). At matched width TNF4 is at PARITY with `fp6 e3m2`:** 51.29 vs **50.29** cells (2 % dearer), **+0.11 / +0.17 pp** accuracy (n.s. on one task), and **96.70 ± 0.38 vs 96.58 ± 0.56** in stability once the quantiser has its gradient-scaling factor | `oracle_rtl_width_matched_2026-08-20.json`, `stability_gradscale_2026-08-20.json` | the earlier 2.97× compared six bits against eight |
| 1a | **Parity is the result.** A novel lattice matching a mature IEEE-style encoding at equal width means the φ-structure costs nothing to adopt | same | not superiority; the paper's current hardware claim is not supported at matched width |
| 1b | **Recipe-insensitivity survives; its EXPLANATION is withdrawn (W949).** Sweep now **45 runs** over five recipe axes: successes TNF4 **45/45**, `fp6 e3m2` **16/45**, `fp6 e2m3` **12/45**. The newest axis is the **scale initialisation**, and it alone flips `fp6 e3m2` from **0/5 to 5/5** while TNF4 moves 0.10 pp | `scaleconv_w949.json`, `stability_*.json`, derived by `verify_numbers.py` (38 checks) | **the range mechanism is withdrawn**: it was computed as min/max of the grid, i.e. peak→format max, while every run mapped peak→1.0, under which TNF4 zeroes 12.5 % against e3m2's 6.25 % and keeps 7 levels against 12. TNF4 wins while handicapped, so we do **not** know why |
| 1c | **RESOLVED AGAINST US (W950). The instability was the RECIPE's, not the format's.** With the OCP-style **computed power-of-two** scale — never learned — **all 30 runs succeed**: block 32 and per-tensor, all three formats. `fp6 e2m3` goes **28/40 failures → 0/5** at unchanged granularity. Paired at per-tensor, TNF4 is **−0.376 pp** vs `fp6 e2m3` (t = −7.24, **0/5** seeds favour TNF4) and −0.250 vs `fp6 e3m2` (t = −5.15) | `blockquant_w950.json`, derived by `verify_numbers.py` (53 checks) | **at six bits, on these tasks, the φ-lattice has no measured advantage over a same-width float and a measured deficit under the standard recipe** |
| 1d | **Mechanism MEASURED, not inferred (W951).** Every run overshoots the top of the grid, so "saturation ⟹ failure" is dead (6.7 % agreement). What separates is **magnitude**: among 45 learned-scale runs the worst success overshoots **1 510×**, the best failure **84 775×** — **non-overlapping**. A computed power-of-two scale bounds the overshoot in **[1,2)** by construction; measured max over 90 runs **2.0000** | `saturation_w951.json`, `sweep_w951_*.json` | supersedes the W950 proxy, which measured a correlated quantity (90.8 %) |
| 1e | **Sweep redone under the deployed recipe (W951): 0 failures in 90 runs**, three tasks × two granularities × three formats × five seeds. Same tasks and seeds under the learned scale: 9 failures | same | TNF4 has still never failed anywhere — a true claim about tolerating a bad recipe, not about deployment |
| 1f | **The 2 %-dearer figure prices decode + a CONSTANT multiply, not a datapath (W952/W953).** Range forces width: TNF4 needs **17** bits per value, **33** per product, a **38-bit** block-32 accumulator, against `fp6 e2m3`'s **7 / 13 / 18**. A fixed-point MAC lane costs **768** cells against **159** — **4.83×**. The part no implementation escapes, the accumulator, is **48 vs 23** cells, i.e. **+0.78 cells per element** amortised over 32 (**≈ +1.5 %**) | `mac_w952.json`, `acc_w952.json`, `widths_w952.json` | **quote the bracket +1.5 % … +383 %, never one end**: the float-style lane (mantissa multiply + exponent add) is **not measured**, and it is the implementation an MX engine most likely uses |
| 1g | **The bracket closes at +46 % (W953).** A float-style lane — multiply odd mantissas, add shifts, align — costs **108 / 82 / 74** cells for TNF4 / `e3m2` / `e2m3`: **1.46×**, not 4.83×. It is cheaper than the fixed-point lane **for every format** (108 vs 768; 74 vs 159), so fixed point is simply the wrong design and its ratio must not be quoted. TNF4 has the **narrowest** multiplier (2-bit odd mantissa) and the **widest** aligner (15 shifts) | `flane_w953.json`, derived by `verify_numbers.py` (91 checks) | the accumulators here are 40/28/24 against the minimal 38/24/18, so all three carry the same small pessimism — the ratio is like-for-like |
| 1h | **At MATCHED RANGE the φ-lattice is at parity, and the +46 % priced range (W954).** `fp6 e4m1` — an ordinary float, same six bits, exponent widened to **15.58** binades against TNF4's 14.58 — costs **106** cells against TNF4's **108**: **+1.9 %**, and carries **31** distinct values against 28. Replicated at ten bits on the ladder's true TNF8: **380 vs 376**, **+1.1 %** | `rangematch_w954.json`, `ladder_w954.json` | **there is no configuration measured here in which the lattice wins** — cost tracks range and mantissa, ≈2.4 cells per binade per lane |
| 1j | **Cost is set by two structural integers, not by range (W962). T806's rate is withdrawn.** Sweeping every exp/mant split at 6 and 10 bits, cost is **not monotone in binades** — `fp6 e1m4` spans *less* range than `fp6 e2m3` and costs *more* (80 vs 74); same inversion at ten bits (230 vs 215). A binade-only regression gives 6.49 cells/binade at R² = 0.85. **The exact law needs no fitting:** formats sharing **(odd-mantissa bits, max shift)** share a bus and an accumulator and cost the same — TNF4/`fp6 e4m1` **108 vs 106**, TNF8/`fp10 e5m4` **380 vs 376** | `curve_w955.json`, derived by `verify_numbers.py` (118 checks) | every format in both the W954 and W955 records reproduces **exactly** across independent runs (106/106, 74/74, 82/82, 376/376) |
| 1i | **The ladder's TNF8 was never measured (W954).** Every rig instantiated `TNFFormat(4, 3)` — **11 bits, 126.91 binades** — while the ladder defines TNF8 as `TNFFormat(3, 4)` — **10 bits, 30.95 binades**. Separately, `LADDER` is bound to **v1-research** while `DEFAULT_LADDER_VERSION` is **v2-spec**, and they disagree above rung 8 (TNF16: **17 vs 19 bits**) | `tnf_ref.py`, verified in-session | **this resolves #644**: 16 by name, 17 by the research ladder, 19 by the spec ladder — three sources, three correct answers about different objects |
| 1k | **The published TNF8 area penalty had the WRONG SIGN (W963).** Re-measured in this project's own metric, the ladder's true rung `TNFFormat(3,4)` is **212.57** consumer cells against a width-matched `fp10 e5m4`'s **214.57** — **0.9 % cheaper**. The substitute `TNFFormat(4,3)` is **270.57** against `fp11 e6m4`'s **257.57** — **5.0 % dearer**. Decoder alone: **12.00 vs 29.00**, 2.4× | `census_tnf8_w963.json`, R² ≥ 0.9996 | in the MAC-lane metric the same rung is **+1.1 %** — two metrics straddling zero, which is the strongest available statement of **parity**. Accuracy for the true rung is still **unmeasured** |
| 1l | **The true eighth rung is at parity on accuracy too, and the substitution cost nothing there (W964).** MNIST, 5 seeds, 3 recipes: TNF8 `(3,4)` vs width-matched `fp10 e5m4` is **−0.010 / −0.018 / +0.060 pp**, none significant, sign changing between recipes. **Zero failures in all 45 runs**, including under the learned scale that destroyed `fp6 e2m3` 28/40 times at six bits | `rung_w964_*.json`, derived by `verify_numbers.py` (147 checks) | **damage is metric-dependent**: the same substitution inverted the *area* sign (1k) and is harmless in accuracy. Rungs 16+ remain unmeasured, and their width depends on which ladder version the caller imported |
| 1m | **Rung 16 is STRICTLY DOMINATED by an ordinary float (W965)** — settled from the grids, no experiment needed. At each width the range-matched float has **exactly** the rung's `(odd, max shift)` pair — **10/135** at 17 bits, **12/137** at 19 — hence exactly its lane cost by T807, while carrying **more values** (131 071 vs 129 025; 524 287 vs 516 097) and **more range** (136 vs 127; 138 vs 127 binades) | `rung16_w965.json`, derived by `verify_numbers.py` (185 checks) | **the cost equality is a prediction from T807**, validated by synthesis at 6 and 10 bits, not measured at 17/19 — a case table over 2¹⁹ codes is not buildable. Value counts and structural parameters are exact |
| 1n | **MEASURED (W966), and it corrects 1m: same grid, 2 % dearer.** 1m's "more values" came from **subnormals**, which TNF does not have — its decoder flushes offset 0 to zero. Rebuilt with the float in TNF's own discipline and priced structurally over **every code**: TNF16 **516 097** values / **127.00** binades / **450.29** cells against `fp19 e7m11` FTZ's **516 096** / **126.00** / **441.29** — **+2.0 %**. At ten bits, TNF8 `(3,4)` **961**/**30.95**/**230.57** against **960**/**29.95**/**225.57** — **+2.2 %**. The gap sits in the **decoder**: 27 vs 18, and 18 vs 13 | `struct966.json`, `structural_w942.json` | TNF16 reproduces the W942 record **exactly** (450.29). The FTZ definition is ours, matched to TNF's own zero/inf handling; the Verilog is verified against its Python reference over every code |
| 1o | **The substitution is a CLASS, and five rigs still carry it (W967).** `tri rungs` resolves every `TNFFormat(…)` call in every rig to its width and rung, across both ladder versions: **34 instantiations, 5 standing alone as non-rungs** — `accuracy_coordinate.py`, `accuracy_seeds.py`, `activations.py`, `conv.py`, `oracle_rtl.py`, all binding "TNF8" to `(4, 3)` at 11 bits | `tri rungs`, run in-session | **damage splits by axis**: the census one inverted the area sign (1k), the four accuracy ones are within ±0.1 pp (1l). Files that also instantiate `(3, 4)` are labelled controls, not defects |
| 1p | **The class is cleared, and the records are annotated (W968).** All five rigs now instantiate `TNFFormat(3, 4)`; `tri rungs` is **wired into `tri audit`** and reports **34 instantiations, 0 standing alone**. Six records carrying an unqualified "TNF8" now hold a `_format_note` naming the actual format and pointing at `census_tnf8_w963.json` (area) and `rung_w964_*.json` (accuracy) | rigs + records in this repo | **two records needed nothing**: `accuracy_coordinate_w938.json` keys `"TNF8 (E_t=4,M=3)"` and `structural_w942.json` stores `physical_bits` — they wrote the **object** into the record, not the name, and aged correctly |
| 1q | **Records regenerated on the true rung; substitution's accuracy cost ≤ 0.07 pp (W969).** `activations.py` re-run with `TNFFormat(3, 4)`: MNIST **−0.008 / +0.018 pp**, Fashion **−0.016 / −0.068 pp**, all |t| ≤ 1.48, sign changing. A **second independent record** confirming the asymmetry — area inverted, accuracy untouched | `activations_w969.json` vs `activations_w941.json`, derived by `verify_numbers.py` (216 checks) | source and data now agree; the older record keeps its `_format_note` |
| 1r | **The replication promise was true for 2 rigs out of 20 (W969).** `FALSIFY-ME.md` has invited outside replication since W948d; **fifteen files still named this machine's directories**. Now **zero** — every rig honours `T27_WORK`/`T27_CONFORMANCE` (and `T27_TNET`/`T27_SYNTH` for the FPGA rigs), and `tri audit` gates it: **32 rigs parse, none names a path outside its tree** | `tri audit` | the automated edit broke `stability.py` — caught by parsing every rig immediately after, not by running one |
| 1s | **Damage assessment complete (W970): one inverted sign, ten accuracy cells, none significant.** Every record from a substituted rig regenerated on `TNFFormat(3, 4)`. Across `activations`, `conv` and `accuracy_seeds` — three rigs, two tasks, five seeds, paired — the largest difference is **0.068 pp** and the largest **|t| = 1.48**, signs mixed | `activations_w969.json`, `conv_w970.json`, `accuracy_seeds_w970.json`, derived by `verify_numbers.py` (222 checks) | **the asymmetry has a mechanism**: area depends on decode-table structure (127 binades vs 31 → decoder 29 vs 18), accuracy on grid density near the working range, which both over-resolve. The class opened in W954 closes here |
| 1t | **The last convention is removed; the answer is two-sided (W972).** 1n's float peer was flush-to-zero — a rule *we* chose. Measured against a float with **real subnormals**, normaliser paid for: TNF16 **450.29** cells against **501.29** — **10.2 % cheaper**, while the float carries **524 287** values to TNF's **516 097**. Against the FTZ peer it remains **+2.0 %** on an identical grid | `struct972.json`, every code verified | **subnormals cost exactly +60 cells and buy 8 191 values** (7.33 cells per thousand). The φ-lattice is a float that has declined subnormals, priced **9 decoder cells above** one that did the same |
| 1u | **"Docker is not running" was wrong for twenty-two waves (W972).** Docker Desktop **is running**; `com.docker.backend` runs under two PIDs; `/var/run/docker.sock` exists as a real socket. **`docker version` hangs** — the daemon accepts the connection and never answers | probed in-session | **wedged, not absent**: the remedy is *quit and reopen*, not *start*. It hid because `docker info` hangs rather than failing, so every probe bundled with other work died as a timeout |
| 1v | **FIRST SILICON NUMBERS (W973): the design meets timing on the real part.** Built from the spec through `t27c silicon` on **`xc7a200tfbg676-1`**: **123 LUT, 52 CARRY4, 0 DSP48E1, BSCANE2 ×1**, **Fmax 80.35 MHz** against a 70.77 MHz target — **PASS with 13.5 % margin**; 1.19 DUT-equivalents of arithmetic reached the die. Bitstream **9 730 834 B**, sync word at offset 230 | `bitstream_w973.json`, derived by `verify_numbers.py` (238 checks) | **synthesis and timing, not a die verdict** — the JTAG chain is empty, so nothing was read back |
| 1w | **The blocker was never Docker (W973).** Four preconditions, measured separately: Docker **wedged** (2 d 1 h, stale socket) not absent; the **native** openxc7 toolchain present all along and **never used by anyone**; the cable present; **the JTAG chain EMPTY — the target does not answer** | `tri bench` | `docker info` **hangs**, killing any command it is bundled into; `--detect` prints **`empty`** for a live cable with a silent board. `tri bench` now prints one verdict per precondition |
| 1x | **The format's own operators on the real part (W974).** Spec → bitstream on `xc7a200tfbg676-1`: `gft_sadd` **1 312 LUT / 18.24 MHz**, `gft_signed_mac` **6 466 / 9.14**, `gft_signed_dot4` **12 872 / 5.50**, all timing **PASS**; the classifier is **123 / 80.35** but on a **different clock**. MHz/kLUT falls **13.9 → 1.41 → 0.43** across the comparable three — a factor of **32** | `silicon_w974.json`, derived by `verify_numbers.py` (262 checks) | **a MHz/LUT headline must name the operator and the clock**; `cfgmclk` and `slowclk` rows are not one ranking. DUT-equivalents (3.10 vs 1.06) are the honest denominator |
| 1y | **`gft_signed_dot4` is incomplete, for two unrelated reasons (W974).** nextpnr hit a **600 s cap — reported `ABSENT`, not `FAIL`**, so nothing was proven unroutable; and a genuine **BSCAN chain/site mismatch** — `JTAG_CHAIN(3)` enabled while the wrapper wires `BSCAN4` | same | **"not measured" and "measured negative" are separate columns**; one red line is evidence about the design, the other about the time budget |
| 2 | **TNF4 is the only sub-8-bit format measured that works under PTQ**: fp4 e2m1 and GF4 lose 70.5 / 71.3 pp | same | GF4 and fp4 are one lattice (identical to the digit on every seed) |
| 2a | **The advantage is a range, not a number.** Without retraining: **13–65 pp**. With max-scale QAT: **+0.19 / +0.89 pp**. With a **learned** scale: **+1.58 / +0.91 pp** — a better recipe *widened* it eightfold on MNIST | `qat_2026-08-20.json`, `lsq_2026-08-20.json` | epoch budgets differ (4 vs 3) across the two QAT runs; paired tests are within-run |
| 2c | **WITHDRAWN and replaced (W945).** The 4-bit comparison was not width-matched: TNF4 is physically **6 bits / 57 grid values**, fp4 e2m1 is 4 bits / 15. Against real 6-bit floats the advantage is **+0.11 (MNIST, t 2.2)** and **+0.17 (Fashion, t 1.2 — not significant)**, and with quantised activations on Fashion **fp6 e3m2 wins by 0.42** | `lsq_width_matched_2026-08-20.json` | four waves of headline numbers were measured against a competitor two bits narrower |
| 2d | **What survives is stability, not accuracy.** TNF4's σ is **0.17–0.72 pp** in every configuration; with quantised activations on MNIST `fp6 e2m3` has **σ = 46.09** and `fp6 e3m2` **σ = 32.33** | same | one architecture, two tasks, three epochs |
| 2b | On a **CNN** the collapse is smaller and unstable: fp4 −13.13 ± 13.66 (MNIST), −25.21 ± 11.31 (Fashion), TNF4 −0.15 / −0.31 | `conv_2026-08-20.json` | two epochs; per-tensor scale over small kernels spans fewer magnitudes |
| 3 | The 4-bit effect **scales on two independent axes** — ×3.3 with task difficulty, ×2.3–4.5 with network capacity; t from 3.7 to 24.7, 5/5 seeds | `accuracy_seeds_2026-08-20.json`, `accuracy_seeds_big_2026-08-20.json` | two MLP sizes, two tasks; no conv, no QAT |
| 4 | **At 8 and 16 bits no format difference reaches the task.** The null has now held across MLP **and CNN**, weights-only **and** weights+activations, two tasks, two sizes, five seeds — largest drop 0.06 pp | same + `accuracy_coordinate_mnist_2026-08-20.json`, `conv_2026-08-20.json` | binomial SE is 0.16–0.25 pp: this is "not discriminative", not "equal" |
| 5 | **The consumer is priced by alphabet width and saturates at its own precision**: 3.43 cells at 2 bits → 385 at 16 → 427 at 32 | `alphabet_width_2026-08-20.json` | one 12×8 multiply as the consumer; a wider one moves the knee |
| 6 | **The decode gap survives fusion exactly** (TNF16 vs BNF16: 8.000 cells bare and fused) **but is 2 % of the unit** | `fusion_2026-08-20.json` | 16-bit modules; see #8 |
| 7 | **Our posit baseline is sound**: `posit16_decode` costs 1.36× PACoGen's `data_extract_v1` while assembling a full fp32 it does not; at operator level TNF's adder is 1.23× cheaper than PACoGen's `posit_add` | `head_to_head_pacogen_2026-08-20.json` | different microarchitecture (handshake vs combinational); correctness not re-verified |
| 8 | **Three physical widths circulate for one rung** — TNF16 is 19 bits by the oracle, 17 by the caption, 16 by a module name | issue #644 | a specification question for the author, not a measurement |
| 9 | **The accuracy multiple is carried by the prior**: TNF16 over posit16 is 14.63× under the published uniform-77-binade draw and 1.02× under a standard normal; TNF16's own error is prior-invariant to 1.046× | `prior_sensitivity_2026-08-20.json` | round-trip error, not task accuracy; LNS16's row is not measured by that path |
| 10 | **The empirical prior of trained weights spans 8.1 binades**, against the 77 the regenerators draw from | `accuracy_coordinate_mnist_2026-08-20.json` | one architecture, two tasks |

## What this project has withdrawn about its own work

- "LNS16 does not reproduce" — **withdrawn** (tf#632). `MATRIX.md:35` lists it at
  43.11 MHz, 0.16 % from the published value; the exception came from `None` cells
  in our own reference table, and the band was applied with a denominator it was
  never defined with.
- A 70-point 4-bit gap — **withdrawn** as an unscaled artefact: fp4 and GF4 flush
  98.8 % of weights to zero without a per-tensor scale.
- A frontier row priced by module name — **withdrawn** (tf#642), the error the
  report existed to expose.
- "A frequency harvested under a slack constraint measures headroom" — **corrected**:
  true for router1, false for router2, which does not consume `--freq`.
- "TNF4 is the only format that works at four bits" — **narrowed** to post-training
  quantisation. Under QAT the gap is 0.19–0.89 pp, not 38–64.

## What no measurement here can settle

Power, energy, on-hardware validation, a vendor-flow calibration of the open
toolchain, and every editorial decision about what the manuscript claims.

---

*φ² + φ⁻² = 3 | TRINITY*
