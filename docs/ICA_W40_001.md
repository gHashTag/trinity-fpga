# ICA-W40-001: OP_SPARSE_MASK Opcode Cascade Rectification

## Summary

`OP_SPARSE_MASK` was minted as `0xE8` in the W40 specification. A parallel W41
wave landed first on master, assigning `0xE8` to `OP_SPARSE_SKIP`. The Coq lane
and Rust lane autonomously shifted `OP_SPARSE_MASK` to `0xED` to resolve the
collision. This ICA aligns the remaining lanes.

## Lanes Affected

| Lane | Repo | Artifact | Status |
|------|------|----------|--------|
| FF (TRUTH) | t27 | `trios-coq/Physics/SparsityMask.v` | 0xED (merge sha `e78816f`) |
| FF'' (TRUTH) | tt-trinity-max-true | `crates/sparsity-witness/` | 0xED (merge sha `8286580`) |
| FF' (this ICA) | trios | `assertions/sparsity_witness.json` | rectified 0xE8→0xED |
| GG (this ICA) | trinity-fpga | `rtl/sparsity/sparse_pe_mask.sv` | rectified 0xE8→0xED |
| FF''' (addendum) | trios | `docs/phd/chapters/glava_91_sparsity.tex` | prose-only, stays 0xE8 + addendum |

## Root Cause

W41 parallel wave landed `OP_SPARSE_SKIP = 0xE8` on master before the W40
merge window closed. The Coq prover (t27 PR `t27#674`) and the Rust crate
(tt-trinity-max-true PR `#37`) both detected the collision and shifted
autonomously to `0xED`. The JSON witness and RTL header were not updated in
the same window.

## Changes Made

### trios — `assertions/sparsity_witness.json`

- `opcode.value`: `"0xE8"` → `"0xED"`
- `opcode.chain_range`: `"0xD0..0xE8"` → `"0xD0..0xED"`
- Root field added: `"ica": "ICA-W40-001: opcode shifted from 0xE8 (occupied
  by OP_SPARSE_SKIP) to 0xED per Coq t27 e78816f + Rust tt-true 8286580 truth"`

### trinity-fpga — `rtl/sparsity/sparse_pe_mask.sv`

- Line 4 header: `OP_SPARSE_MASK=0xE8` → `OP_SPARSE_MASK=0xED`
- Line 8 comment: `OP_SPARSE_MASK = 0xE8` → `OP_SPARSE_MASK = 0xED`
- ICA comment appended immediately after line 4:
  `// ICA-W40-001: shifted from 0xE8 (taken by OP_SPARSE_SKIP) to 0xED per Coq+Rust truth`

## Truth Anchors

- Coq master merge SHA: `e78816f` (t27 PR #674)
- Rust master merge SHA: `8286580` (tt-trinity-max-true PR #37)
- Refs: trios#906 (W40 ONE SHOT), trios#907, trinity-fpga#155 (W40 ONE SHOT), trinity-fpga#156

## Constitutional Compliance

- R1..R18 preserved; no new R{19+} introduced.
- Coq `.v` and Rust `.rs` files are TRUTH — not modified.
- Apache-2.0 license unchanged.
- Commit identity: `Dmitrii Vasilev <admin@t27.ai>`.

## PhD Addendum Note

`glava_91` (`docs/phd/chapters/glava_91_sparsity.tex`) is prose-only. It may
retain `0xE8` with an addendum note explaining the ICA-W40-001 shift. No
structural changes required to the TeX source.

---

phi^2 + phi^-2 = 3 · QUANTUM BRAIN 1:1 SILICON · NEVER STOP · DOI 10.5281/zenodo.19227877
