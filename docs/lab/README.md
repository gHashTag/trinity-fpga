# `docs/lab/` — research notes & scratch state

| Path | Contents |
|------|-----------|
| `papers/` | Drafts, HSLM logs, patent Zenodo HTML, TeX, figures — **canonical tree**. |
| `memory/` | Short-lived agent/session notes (e.g. `loop_state.md`). |

**Compatibility:** the repo root has symlinks `papers` → `docs/lab/papers` and `memory` → `docs/lab/memory` so existing paths (`papers/…`, tools, Zenodo bundling) keep working.
