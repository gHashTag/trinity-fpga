# HW Campaign — AX7203 (connected), 2026-07-30

> Handoff for the **local hardware agent** working in `gHashTag/trinity-fpga`
> (branch `main`). Prepared by the sandbox agent after **verifying every path
> against the real repo** (no hallucinations — corrections in §0).

## Environment status (this machine, checked 2026-07-30)
- **Board DETECTED**: `/dev/cu.usbserial-1110` and `/dev/cu.usbserial-210512180081`
  (CP2102N USB-serial → matches AX7203 UART TX=N15/RX=P20).
- **`openocd` present** (`/opt/homebrew/bin/openocd`); configs present:
  `fpga/openxc7-synth/ax7203_al321.cfg`, `fpga/openxc7-synth/openocd.cfg`.
- **`yosys` present** (0.63, git sha 70a11c6).
- **`nextpnr-xilinx` ABSENT** and **no `openxc7` Docker image** →
  **Task D (full P&R) CANNOT run here until nextpnr-xilinx is installed.**
  Building NEW bitstreams (Tasks E, B) is likewise blocked without it.
- Some **pre-built `.bit` files are committed** (e.g. `artifacts/bitstreams/gf8_clean_ax7203.bit` + `.sha256`,
  `fpga/openxc7-synth/corona_compute_gf16_add_ax7203.bit`) — these are flashable as-is.

## Board (BINDING)
ALINX AX7203 = Xilinx `xc7a200tfbg484-2`, IDCODE `0x13636093`, 200 MHz LVDS
clock R4(+)/T4(−) via IBUFDS→BUFG, UART @160000 baud. Chips/ASIC/Tiny Tapeout:
none — FPGA only.

## Tier-E = full 4/4 chain (BINDING; nothing less counts)
1. CI run GREEN — public URL `https://github.com/gHashTag/trinity-fpga/actions/runs/<id>`
2. bitstream SHA-256
3. UART log `HW RESULT: N/N bit-exact (fails=0)` @160000
4. IDCODE `0x13636093` written in the body of comment #199

A GREEN commit label ≠ Tier-E. iverilog sim-bitexact ≠ HW. yosys-PASS ≠ Tier-E.
Keep three metrics separate: RTL-generated ≠ yosys-PASS([simulated]) ≠ Tier-E 4/4.
No "first/best/only". Status tags required. `confirm_action` before any push/merge.

## §0 Path corrections to the original campaign instruction (verified)
The original instruction named designs that **do not exist**:
- ❌ `gf16_add_ax7203.v` — use **`fpga/openxc7-synth/corona_compute_gf16_add_ax7203.v`** (or dedicated `gf16_add.v`).
- ❌ `gf16_mul_ax7203.v` — use **`fpga/openxc7-synth/corona_compute_gf16_mul_ax7203.v`** (or dedicated `gf16_mul.v`).
- ❌ `gHashTag/t27/fpga/vivado/gf16_matmul4x4_top.v` — **no matmul design exists in the repo at all**; drop this target from Task D or name the real top.
Verified-present assets the campaign relies on:
- `conformance/test_all_77_hw.sh` ✓ (batch HW driver, includes gf48/64/96/128, `test_type=dedicated`)
- 10 `_sub_` RTL ✓ : `fpga/vivado/gf{4,6,8,10,12,14,16,20,24,32}_sub_ax7203.v`
- host scripts ✓ : `conformance/{takum32,takum64,gf128}_decode_conformance_ax7203.py`, `conformance/gf{N}_sub_conformance_ax7203.py`
- `fpga/openxc7-synth/gfplus8_a_decode.v` ✓

## Priority order
**D (P&R numbers) → C (takum IDCODE) → A (decode remainder) → E (SUB) → B (compute remainder).**
D unblocks the article's biggest weakness (the [method-unpinned] LUT axis).

### Task D — P&R utilization on AX7203 [HIGHEST; BLOCKED on nextpnr here]
Full openXC7 P&R (nextpnr-xilinx) for `xc7a200tfbg484-2` on the REAL targets:
`corona_compute_gf16_add_ax7203.v`, `corona_compute_gf16_mul_ax7203.v`,
`gfplus8_a_decode.v`, (drop the non-existent matmul).
From the post-route report capture: **LUT, FF, DSP, Fmax (critical path), Slice**.
**Protocol (BINDING — otherwise the ~3× trap of PAPER_INTEGRITY §H/§I repeats):**
state the exact top module; set params explicitly (NOT defaults — `gf_adder_param`
default = GF14, `gf_mul_param` default = GF16); state registered-output vs
`result_comb` FORMAL-tap; with/without `-flatten` (we know it doesn't affect
routing — confirm on silicon). One summary table "design × {LUT,FF,DSP,Fmax,protocol}"
+ attach the post-route report. Deliverable → replaces `[method-unpinned]` in both
articles with `[measured — post-route P&R on AX7203]`.
**This sandbox cannot run D** (no nextpnr-xilinx).

### Task C — capture IDCODE for takum32/64 (chain 3.5/4 → 4/4)
Existing proofs #4917131487 / #4917211175 = CI + SHA256 + UART 65/65 & 45/45
fails=0, but IDCODE `0x13636093` is missing from the #199 body. Flash the
takum32/takum64 bitstreams, re-read the IDCODE line per bitstream, append
`IDCODE 0x13636093` to the existing #199 proof comments. Only then may the
Tier-E counts be revised (not retroactively). If clean → takum32/64 leave horizon-B.

> **Honesty note on the old "71/83" headline:** "71/83" was a decode+compute
> CELL SUM, not format-coverage. EPIC #199 @2026-07-15 retracted it
> (`"71/83" was WRONG` — double-counted cells as formats). The real union
> (decode ∪ compute) is ~49-55/83. This handoff no longer treats 71/83 as a
> coverage ceiling; #199 is the live source.

### Task A — decode remainder: gf48 / gf64 / gf96 / gf128 (CI-proven, UART-pending)
Bitstreams are CI-proven; UART not yet captured. Run
`conformance/test_all_77_hw.sh` (power-cycle to unblock JTAG if needed) → capture
UART for gf48/64/96/128. For EACH format, write the full 4/4 chain on #199
(CI URL + SHA256 + UART N/N fails=0 + IDCODE). ⚠️ The script checks UART only —
SHA256 and IDCODE must be captured separately by hand. Expected: decode-HW +4.

### Task E — compute SUB for 10 GF [NEW — RTL exists]
`_sub_` RTL exists: `fpga/vivado/gf{4,6,8,10,12,14,16,20,24,32}_sub_ax7203.v`
(this corrects the old "no SUB RTL" note). Build each bitstream (openXC7 CI),
flash, run `gf{N}_sub_conformance_ax7203.py --op sub`, capture UART, write 4/4 on
#199 per (cell, sub). Expands compute from ADD+MUL → ADD+SUB+MUL ("full op triple
on FPGA silicon"). Needs nextpnr to build.

### Task B — compute remainder: gf256 / gf8_bfp / gf_lns_hybrid (+ ready gfternary)
`gfternary_compute_conformance_ax7203.py` already exists → gfternary removed from
the remainder (run it directly). Remaining 3 without a compute-host:
gf256, gf8_bfp, gf_lns_hybrid (all have add/mul/alu/cmp/fma/to_fp32 RTL +
CI-workflows + decode-host). Clone the template
`conformance/gf16_compute_conformance_ax7203.py` for each (`--op {add,mul}`).
Build (openXC7 CI; a hanging `docker pull regymm/openxc7` is Docker-Hub timing,
not a workflow bug — retry), flash, run compute-conformance on board (incl. the
ready gfternary), capture UART, write 4/4 per (cell, op). Expected: compute-HW +4
GF × {ADD, MUL}.

## Reporting rules (BINDING)
git push/PR via `api_credentials=["github"]` (whoami=gHashTag); local identity:
`git -c user.email=admin@t27.ai -c user.name=gHashTag` (NOT `--global`). Check
HEAD/CI via public `api.github.com/repos/gHashTag/trinity-fpga/actions/runs`
(background `gh api` 404 is a known git-agent-proxy limit, not a breakage).
Comments to #199 via `gh api issues/199/comments` work. Red `fpga-ci.yml` =
legacy VIBEE/Zig (no build.zig) — known, not a regression, unrelated to the
format bitstreams (those are the green `ax7203-corona-*` workflows).

## Note from the preparing agent
This sandbox agent did **NOT** flash the board (no per-action confirmation, and
Task D is blocked by the absent `nextpnr-xilinx`). The board IS detected and
`openocd` IS available, so the local HW agent can proceed. The software LUT axis
(`research/PAPER_INTEGRITY_ISSUES.md` §H/§I, `scripts/lut_measure.sh`,
`paper.tex` Reproducibility caveat) is already characterised — Task D's
post-route numbers will replace the yosys-pre-P&R figures directly.
