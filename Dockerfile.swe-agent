# Trinity SWE Agent — Railway Dev Agent Container
# Pure Zig entrypoint: reads issue → runs pipeline → creates PR
# Needs: git, gh, curl, zig (for build validation)

FROM ubuntu:24.04

RUN apt-get update && apt-get install -y \
    ca-certificates curl wget git xz-utils \
    && rm -rf /var/lib/apt/lists/*

# Install GitHub CLI
RUN curl -fsSL https://cli.github.com/packages/githubcli-archive-keyring.gpg \
    | dd of=/usr/share/keyrings/githubcli-archive-keyring.gpg \
    && echo "deb [arch=$(dpkg --print-architecture) signed-by=/usr/share/keyrings/githubcli-archive-keyring.gpg] https://cli.github.com/packages stable main" \
    | tee /etc/apt/sources.list.d/github-cli.list > /dev/null \
    && apt-get update && apt-get install -y gh \
    && rm -rf /var/lib/apt/lists/*

# Install Zig 0.15.2 (needed for zig build + zig build test validation)
RUN wget -q https://ziglang.org/builds/zig-linux-x86_64-0.15.0-dev.650+2d4c32689.tar.xz -O /tmp/zig.tar.xz \
    && tar xf /tmp/zig.tar.xz -C /opt \
    && ln -s /opt/zig-linux-x86_64-0.15.0-dev.650+2d4c32689/zig /usr/local/bin/zig \
    && rm /tmp/zig.tar.xz

# Pre-built SWE entrypoint binary (cross-compiled for x86_64-linux)
COPY prebuilt/swe-entrypoint-bin /usr/local/bin/swe-entrypoint

RUN chmod a+rx /usr/local/bin/swe-entrypoint

WORKDIR /workspace

ENTRYPOINT ["/usr/local/bin/swe-entrypoint"]

LABEL version="2.0.0" \
      description="Trinity SWE Agent — autonomous dev agent for GitHub issues" \
      trinity.identity="phi^2 + 1/phi^2 = 3"
