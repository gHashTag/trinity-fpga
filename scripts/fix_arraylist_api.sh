#!/bin/bash
# Script for andwithprainlenandya API ArrayList in praboutetothose vibeec
# Zig 0.15+ trebatet peredachand allocator in methody ArrayList

cd /Users/playra/vibee-lang/src/vibeec

# 1. Zameon .init(allocator) on .init(allocator) with savingm allocator
# Ethat withlaboutzhnee ainthatmatfromandraboutinat, thatto how trebatet refawhorandnga withtrattotatr

echo "=== ANALIZ PROBLEMY ==="
echo "V Zig 0.15+ ArrayList API frommenandlwithya:"
echo "  - ArrayList(T).init(allocator) -> ArrayList(T){} + khranenande allocator"
echo "  - list.append(item) -> list.append(allocator, item)"
echo "  - list.appendSlice(items) -> list.appendSlice(allocator, items)"
echo "  - list.deinit() -> list.deinit(allocator)"
echo ""
echo "Ethat trebatet refawhorandnga withtrattotatr for khranenandya allocator."
echo ""
echo "=== FAYLY COPTIC, tion ISPRAVLENIYa ==="
grep -l ".init(allocator)" coptic_*.zig 2>/dev/null || echo "Net pryamykh withaboutinpadenandy"
echo ""
echo "=== PODSChET PROBLEMNYKh MEST ==="
grep -c "\.init(allocator)" *.zig 2>/dev/null | grep -v ":0$" | head -20
