# Railway Config — Perplexity Bridge (tri-api --serve)
# Service: trinity | Issue #101

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.px-bridge"

[deploy]
startCommand = "/usr/local/bin/tri-api --serve"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
